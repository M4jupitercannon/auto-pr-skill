<!--
Generic PR body template used as a fallback when a project profile does not
provide a `pr_skill_path`. When pr_skill_path is set, the auto-pr-pr-submitter
agent reads that skill and renders its own body matching the project's PR
template (e.g. Paddle's PULL_REQUEST_TEMPLATE.md). This file is only used
verbatim for projects without a custom PR convention.
-->

## Summary

{{SUMMARY}}

## Why

{{MOTIVATION}}

## Affected tests

{{TESTS_LIST}}

## Risk / human review

{{TRIAGE_NOTE}}

---
_Generated by [auto-pr-skill]. Task `{{TASK_ID}}` from run `{{RUN_DIR}}`._
